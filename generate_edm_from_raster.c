#include <stdio.h>
#include "entities.h"
#define BLACK 0
#define WHITE 1
#define SUCCESS 0
#define FAIL 1

int read_raster_image(FILE *raster_image) {
    return SUCCESS;    
}

EDP_t *generate_edp_from_pixel(uint8_t raster_pixel, point_t raster_pixel_point, ) {
	return NULL;
}

EDM_t *generate_edm_from_raster(EDP_t* light_sources, size_t light_sources_size) {
	if (raster_image == NULL) {
	    	return NULL;	
	}
}
